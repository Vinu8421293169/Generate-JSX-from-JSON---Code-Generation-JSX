import React from "react";

export default class Circle {
  color = null;

  constructor(color) {
    this.color = color;
  }

  calculateArea = function () {};
  drawShape = function () {};
}
